import React, {useEffect, useState} from 'react';
import styled from 'styled-components';

interface Props {

}

export default function ImagesTable(props: Props) {
  const [value, setValue] = useState(0);

  return (
    <div>
  
    </div>
  )
}

